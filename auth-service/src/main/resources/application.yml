spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rateguard_auth
    username: postgres
    password: 2435

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true